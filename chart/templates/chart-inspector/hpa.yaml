{{- if .Values.chartInspector.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "chart-inspector.fullname" . }}
  labels:
    {{- include "chart-inspector.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "chart-inspector.fullname" . }}
  minReplicas: {{ .Values.chartInspector.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.chartInspector.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.chartInspector.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.chartInspector.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.chartInspector.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.chartInspector.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
